<!DOCTYPE html>
<html>
<head>
    <% include ../partials/head%>
<meta charset="utf-8" />
<title>CSS3 Contact Form</title>
</head>
<body>
<body class="container">

    <header>
        <% include ../partials/header %>
    </header>

    <main>
        <div class="jumbotron">
            <div id="contact">

                <h1>Data page</h1>
                <p>The datapage is currently in the works</p>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Firstname</th>
                            <th>Lastname</th>
                        </tr>
                    </thead>
                    <% for(var i=0; i < information.length; i++) { %>
                        <tr>
                            <td><%= information[i].ID %></td>
                            <td><%= information[i].Name %></td>
                        </tr>
                    <% } %>
                </table>                
        </div>
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>

    
</div>
</body>
</html>